const config = {
    baseApi:'api/v1',

    port: 8888,

    database: {
        DATABASE: 'test2',
        USERNAME: 'root',
        PASSWORD: '123456',
        PORT: '3306',
        HOST: '127.0.0.1'
    }
}

module.exports = config